[pytest]
addopts = -v
markers =
    show_test: Generate HTML charts for test cases
    slow: Tests that may take longer to run (deselect with '-m "not slow"')
filterwarnings =
    # Silence Plotly/Kaleido engine deprecation warnings during tests
    ignore::DeprecationWarning:plotly\.io\._kaleido
